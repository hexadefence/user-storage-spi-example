**User Storage SPI Example**

Build Command: `./gradlew build`

jar file is already availabe in `build/libs` folder
you can copy that jar file directly to the `/standalone/deployments`
folder of the keycloak server.

Note: This jar file supports Keycloak server version 13.
This is not tested with other versions.

email: hexadefence@gmail.com